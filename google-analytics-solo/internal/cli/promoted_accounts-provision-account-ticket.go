// Copyright 2026 Pooria Arab and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newAccountsProvisionAccountTicketPromotedCmd(flags *rootFlags) *cobra.Command {
	var bodyAccountCreateTime string
	var bodyAccountDeleted bool
	var bodyAccountDisplayName string
	var bodyAccountName string
	var bodyAccountRegionCode string
	var bodyAccountUpdateTime string
	var bodyRedirectUri string

	cmd := &cobra.Command{
		Use:         "accounts-provision-account-ticket",
		Short:       "Requests a ticket for creating an account.",
		Long:        "Requests a ticket for creating an account.",
		Example:     "  google-analytics-solo-pp-cli accounts-provision-account-ticket",
		Annotations: map[string]string{"pp:endpoint": "accounts-provision-account-ticket.analyticsadmin", "pp:method": "POST", "pp:path": "https://analyticsadmin.googleapis.com/v1alpha/accounts:provisionAccountTicket"},
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "https://analyticsadmin.googleapis.com/v1alpha/accounts:provisionAccountTicket"
			params := map[string]string{}
			// HasStore + non-GET falls through to a live API call here
			// rather than through resolveRead (GET-only internally); a
			// body-aware cached read helper is filed as #425 for when a
			// second store-backed POST-search consumer ships.
			bodyMap := map[string]any{}
			var body any = bodyMap
			{
				nestedAccount := map[string]any{}
				if bodyAccountCreateTime != "" {
					nestedAccount["createTime"] = bodyAccountCreateTime
				}
				if cmd.Flags().Changed("account-deleted") {
					nestedAccount["deleted"] = bodyAccountDeleted
				}
				if bodyAccountDisplayName != "" {
					nestedAccount["displayName"] = bodyAccountDisplayName
				}
				if bodyAccountName != "" {
					nestedAccount["name"] = bodyAccountName
				}
				if bodyAccountRegionCode != "" {
					nestedAccount["regionCode"] = bodyAccountRegionCode
				}
				if bodyAccountUpdateTime != "" {
					nestedAccount["updateTime"] = bodyAccountUpdateTime
				}
				if len(nestedAccount) > 0 {
					bodyMap["account"] = nestedAccount
				}
			}
			if bodyRedirectUri != "" {
				bodyMap["redirectUri"] = bodyRedirectUri
			}
			data, statusCode, err := c.PostWithParams(cmd.Context(), path, params, body)

			if err != nil {
				return classifyAPIError(err, flags)
			}
			prov := attachFreshness(DataProvenance{Source: "live"}, flags)
			var partialFailure *partialFailureReport
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 {
				partialFailure = detectPartialFailure(data)
			}
			if !flags.dryRun && statusCode >= 200 && statusCode < 300 && (partialFailure == nil || flags.allowPartialFailure) {
				writeMutationResponseToStore(cmd.Context(), "accounts-provision-account-ticket", data, "")
			}
			// Print provenance to stderr for human-facing output only.
			// Machine-format flags (--json, --csv, --compact, --quiet, --plain,
			// --select) and piped stdout suppress this line; the JSON envelope
			// already carries meta.source for those consumers.
			// SYNC: keep this gate aligned with command_endpoint.go.tmpl.
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope. --select wins over
			// --compact when both are set; --compact only runs when no explicit
			// fields were requested. Explicit format flags (--csv, --quiet, --plain)
			// opt out of the auto-JSON path so piped consumers that asked for a
			// non-JSON format reach the standard pipeline below.
			if flags.asJSON || (!isTerminal(cmd.OutOrStdout()) && !flags.csv && !flags.quiet && !flags.plain) {
				filtered := data
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				} else if flags.compact {
					filtered = compactFields(filtered)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlagsMeta(cmd.OutOrStdout(), data, flags, map[string]any{"source": "live"})
		},
	}
	cmd.Flags().StringVar(&bodyAccountCreateTime, "account-create-time", "", "Output only. Time when this account was originally created.")
	cmd.Flags().BoolVar(&bodyAccountDeleted, "account-deleted", false, "Output only. Indicates whether this Account is soft-deleted or not.")
	cmd.Flags().StringVar(&bodyAccountDisplayName, "account-display-name", "", "Required. Human-readable display name for this account.")
	cmd.Flags().StringVar(&bodyAccountName, "account-name", "", "Output only. Resource name of this account. Format: accounts/{account} Example: 'accounts/100'")
	cmd.Flags().StringVar(&bodyAccountRegionCode, "account-region-code", "", "Country of business. Must be a Unicode CLDR region code.")
	cmd.Flags().StringVar(&bodyAccountUpdateTime, "account-update-time", "", "Output only. Time when account payload fields were last updated.")
	cmd.Flags().StringVar(&bodyRedirectUri, "redirect-uri", "", "Redirect URI where the user will be sent after accepting Terms of Service.")

	// Wire sibling endpoints and sub-resources as subcommands

	return cmd
}
