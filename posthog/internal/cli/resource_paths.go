// Copyright 2026 Pooria Arab and contributors. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"strconv"
	"strings"
)

type resourceReadConfig struct {
	responsePath   string
	paginationType string
	cursorParam    string
	limitParam     string
	nextCursorPath string
	hasMoreField   string
	pageSize       int
}

var resourceReadPaths = map[string]string{ // #nosec G101 -- endpoint paths, not credentials.
	"customer-analytics":            "/api/customer_analytics/external/accounts",
	"llm-analytics":                 "/api/llm_analytics/@me/spend/",
	"organizations":                 "/api/organizations/",
	"public-hog-function-templates": "/api/public_hog_function_templates/",
	"reminders":                     "/api/reminders/",
	"users":                         "/api/users/",
}

var resourceDetailPaths = map[string]string{ // #nosec G101 -- endpoint paths, not credentials.
	"advanced-activity-logs":     "/api/projects/{project_id}/advanced_activity_logs/available_filters/",
	"batch-exports":              "/api/projects/{project_id}/batch_exports/test/",
	"cohorts":                    "/api/projects/{project_id}/cohorts/activity/",
	"comments":                   "/api/projects/{project_id}/comments/count/",
	"dashboard-templates":        "/api/projects/{project_id}/dashboard_templates/json_schema/",
	"data-modeling-jobs":         "/api/projects/{project_id}/data_modeling_jobs/recent/",
	"data-warehouse":             "/api/projects/{project_id}/data_warehouse/check-database-name/",
	"endpoints":                  "/api/projects/{project_id}/endpoints/materialization_conditions/",
	"engineering-analytics":      "/api/projects/{project_id}/engineering_analytics/broken_tests/",
	"error-tracking":             "/api/projects/{project_id}/error_tracking/fingerprints/resolve/",
	"event-definitions":          "/api/projects/{project_id}/event_definitions/golang/",
	"event-filter":               "/api/projects/{project_id}/event_filter/",
	"events":                     "/api/projects/{project_id}/events/values/",
	"experiments":                "/api/projects/{project_id}/experiments/stats/",
	"external-data-sources":      "/api/projects/{project_id}/external_data_sources/connect_link/",
	"feature-flags":              "/api/projects/{project_id}/feature_flags/evaluation_reasons/",
	"file-system":                "/api/projects/{project_id}/file_system/log_view/",
	"groups":                     "/api/projects/{project_id}/groups/activity/",
	"health-issues":              "/api/projects/{project_id}/health_issues/summary/",
	"hog-flows":                  "/api/projects/{project_id}/hog_flows/email_sending_suspension/",
	"hog-functions":              "/api/projects/{project_id}/hog_functions/icon/",
	"insights":                   "/api/projects/{project_id}/insights/my_last_viewed/",
	"integrations":               "/api/projects/{project_id}/integrations/authorize/",
	"js-snippet":                 "/api/projects/{project_id}/js-snippet/resolve/",
	"llm-analytics":              "/api/projects/{project_id}/llm_analytics/evaluation_config/",
	"llm-skills":                 "/api/projects/{project_id}/llm_skills/marketplace/install-command/",
	"logs":                       "/api/projects/{project_id}/logs/has_logs/",
	"marketing-analytics":        "/api/projects/{project_id}/marketing_analytics/data_sources/",
	"mcp-analytics":              "/api/projects/{project_id}/mcp_analytics/sessions/activity_overview/",
	"mcp-gateway":                "/api/projects/{project_id}/mcp_gateway/audit/counts/",
	"mcp-server-installations":   "/api/projects/{project_id}/mcp_server_installations/authorize/",
	"messaging-preferences":      "/api/projects/{project_id}/messaging_preferences/export_opt_outs_csv/",
	"metrics":                    "/api/projects/{project_id}/metrics/has_metrics/",
	"notebooks":                  "/api/projects/{project_id}/notebooks/activity/",
	"object-media-previews":      "/api/projects/{project_id}/object_media_previews/preferred_for_event/",
	"organizations":              "/api/organizations/{id}/",
	"persons":                    "/api/projects/{project_id}/persons/activity/",
	"property-access-controls":   "/api/projects/{project_id}/property_access_controls/",
	"property-definitions":       "/api/projects/{project_id}/property_definitions/seen_together/",
	"query":                      "/api/projects/{project_id}/query/draft_sql/",
	"reminders":                  "/api/reminders/{id}/",
	"session-summaries":          "/api/projects/{project_id}/session_summaries/config/",
	"sessions":                   "/api/projects/{project_id}/sessions/property_definitions/",
	"signals":                    "/api/projects/{project_id}/signals/reports/refund-summary/",
	"stamphog":                   "/api/projects/{project_id}/stamphog/repo_configs/install_info/",
	"subscriptions":              "/api/projects/{project_id}/subscriptions/summary_quota/",
	"surveys":                    "/api/projects/{project_id}/surveys/activity/",
	"tasks":                      "/api/projects/{project_id}/tasks/active_wizard_run/",
	"tracing":                    "/api/projects/{project_id}/tracing/spans/has_spans/",
	"users":                      "/api/users/{uuid}/",
	"vision":                     "/api/projects/{project_id}/vision/quota/",
	"visual-review":              "/api/projects/{project_id}/visual_review/runs/counts/",
	"warehouse-dag":              "/api/projects/{project_id}/warehouse_dag/",
	"web-analytics":              "/api/projects/{project_id}/web_analytics/recap/",
	"web-analytics-achievements": "/api/projects/{project_id}/web_analytics_achievements/overview/",
	"web-vitals":                 "/api/projects/{project_id}/web_vitals/",
	"wizard":                     "/api/projects/{project_id}/wizard/sessions/stream/",
}

var resourceWritePaths = map[string]string{ // #nosec G101 -- endpoint paths, not credentials.
	"code":          "/api/code/invites/redeem/",
	"organizations": "/api/organizations/",
	"reminders":     "/api/reminders/",
	"users":         "/api/users/request_email_verification/",
}

var resourceReadConfigs = map[string]resourceReadConfig{
	"customer-analytics":            {responsePath: "results", paginationType: "cursor", cursorParam: "cursor", limitParam: "limit", nextCursorPath: "next_cursor", hasMoreField: "", pageSize: 100},
	"llm-analytics":                 {responsePath: "", paginationType: "offset", cursorParam: "", limitParam: "limit", nextCursorPath: "", hasMoreField: "", pageSize: 50},
	"organizations":                 {responsePath: "results", paginationType: "offset", cursorParam: "offset", limitParam: "limit", nextCursorPath: "", hasMoreField: "", pageSize: 100},
	"public-hog-function-templates": {responsePath: "results", paginationType: "offset", cursorParam: "offset", limitParam: "limit", nextCursorPath: "", hasMoreField: "", pageSize: 100},
	"reminders":                     {responsePath: "results", paginationType: "offset", cursorParam: "offset", limitParam: "limit", nextCursorPath: "", hasMoreField: "", pageSize: 100},
	"users":                         {responsePath: "results", paginationType: "offset", cursorParam: "offset", limitParam: "limit", nextCursorPath: "", hasMoreField: "", pageSize: 100},
}

func resourceReadPath(resource string) (string, error) {
	if path, ok := resourceReadPaths[resource]; ok {
		return path, nil
	}
	return "", fmt.Errorf("unknown readable resource %q", resource)
}

func resourceWritePath(resource string) (string, error) {
	if path, ok := resourceWritePaths[resource]; ok {
		return path, nil
	}
	return "", fmt.Errorf("unknown writable resource %q", resource)
}

func resourceDetailPath(resource, escapedID string) (string, error) {
	path, ok := resourceDetailPaths[resource]
	if !ok {
		return "", fmt.Errorf("resource %q does not declare a single-item read path", resource)
	}
	start := strings.Index(path, "{")
	end := strings.Index(path, "}")
	if start < 0 || end <= start {
		return "", fmt.Errorf("resource %q has an invalid single-item read path", resource)
	}
	return path[:start] + escapedID + path[end+1:], nil
}

func resourceReadConfigFor(resource string) resourceReadConfig {
	return resourceReadConfigs[resource]
}

func extractResourcePage(data json.RawMessage, config resourceReadConfig) ([]json.RawMessage, string, bool) {
	if items, ok := resourceJSONArray(data); ok {
		return items, "", false
	}

	var envelope map[string]json.RawMessage
	if json.Unmarshal(data, &envelope) != nil {
		return nil, "", false
	}
	if config.responsePath != "" {
		if payload, ok := responsePayloadAtPath(data, config.responsePath); ok {
			if items, ok := resourceJSONArray(payload); ok {
				next, hasMore := resourcePagination(data, envelope, config)
				return items, next, hasMore
			}
		}
	}
	for _, key := range []string{"data", "results", "items", "records", "nodes", "entries", "features", "Data", "Results", "Items", "Records", "Nodes", "Entries", "Features"} {
		if items, ok := resourceJSONArray(envelope[key]); ok {
			next, hasMore := resourcePagination(data, envelope, config)
			return items, next, hasMore
		}
	}
	for _, key := range []string{"data", "result", "Data", "Result"} {
		var nested map[string]json.RawMessage
		if json.Unmarshal(envelope[key], &nested) != nil {
			continue
		}
		for _, nestedKey := range []string{"data", "results", "items", "records", "nodes", "entries", "features", "Data", "Results", "Items", "Records", "Nodes", "Entries", "Features"} {
			if items, ok := resourceJSONArray(nested[nestedKey]); ok {
				next, hasMore := resourcePagination(data, envelope, config)
				return items, next, hasMore
			}
		}
		if items, ok := resourceSingleArray(nested); ok {
			next, hasMore := resourcePagination(data, envelope, config)
			return items, next, hasMore
		}
	}
	if items, ok := resourceSingleArray(envelope); ok {
		next, hasMore := resourcePagination(data, envelope, config)
		return items, next, hasMore
	}
	return nil, "", false
}

func resourceSingleArray(envelope map[string]json.RawMessage) ([]json.RawMessage, bool) {
	var only []json.RawMessage
	arrays := 0
	for key, raw := range envelope {
		if resourceEnvelopeMetadataKey(key) {
			continue
		}
		if items, ok := resourceJSONArray(raw); ok {
			only = items
			arrays++
			continue
		}
		if strings.TrimSpace(string(raw)) != "null" {
			return nil, false
		}
	}
	return only, arrays == 1
}

func resourceJSONArray(data json.RawMessage) ([]json.RawMessage, bool) {
	if len(data) == 0 || strings.TrimSpace(string(data)) == "null" {
		return nil, false
	}
	var items []json.RawMessage
	if json.Unmarshal(data, &items) != nil {
		return nil, false
	}
	return items, true
}

func resourcePagination(data json.RawMessage, envelope map[string]json.RawMessage, config resourceReadConfig) (string, bool) {
	declaredMore := false
	if config.hasMoreField != "" {
		if raw, ok := responsePayloadAtPath(data, config.hasMoreField); ok {
			var value bool
			if json.Unmarshal(raw, &value) == nil {
				if !value {
					return "", false
				}
				declaredMore = true
			}
		}
	}
	if config.nextCursorPath != "" {
		if raw, ok := responsePayloadAtPath(data, config.nextCursorPath); ok {
			if value := resourceCursorValue(raw); value != "" {
				return value, true
			}
		}
	}
	envelopes := []map[string]json.RawMessage{envelope}
	for i := 0; i < len(envelopes) && i < 16; i++ {
		for _, key := range []string{"data", "result", "meta", "pagination", "Data", "Result", "Meta", "Pagination"} {
			var nested map[string]json.RawMessage
			if json.Unmarshal(envelopes[i][key], &nested) == nil {
				envelopes = append(envelopes, nested)
			}
		}
	}
	for _, candidate := range envelopes {
		for _, key := range []string{config.cursorParam, "next_cursor", "nextCursor", "next_token", "nextToken", "next_page_token", "nextPageToken"} {
			if key == "" {
				continue
			}
			raw, ok := candidate[key]
			if !ok {
				continue
			}
			if value := resourceCursorValue(raw); value != "" {
				return value, true
			}
		}
	}
	if declaredMore {
		return "", true
	}
	for _, candidate := range envelopes {
		for _, key := range []string{"has_more", "hasMore", "has_next", "hasNext"} {
			var value bool
			if raw, ok := candidate[key]; ok && json.Unmarshal(raw, &value) == nil {
				return "", value
			}
		}
	}
	return "", false
}

func resourceCursorValue(raw json.RawMessage) string {
	var value string
	if json.Unmarshal(raw, &value) == nil && value != "" {
		return value
	}
	var number json.Number
	if json.Unmarshal(raw, &number) == nil && number.String() != "" {
		return number.String()
	}
	return ""
}

func resourceEnvelopeMetadataKey(key string) bool {
	switch key {
	case "total", "totalResults", "total_count", "count", "size", "links", "meta", "pagination", "next_cursor", "nextCursor", "next_token", "nextToken", "next_page_token", "nextPageToken", "has_more", "hasMore", "has_next", "hasNext", "errors", "warnings":
		return true
	default:
		return false
	}
}

func resourcePageParams(config resourceReadConfig, cursor string, page int, remaining int) map[string]string {
	params := map[string]string{}
	pageSize := config.pageSize
	if pageSize <= 0 {
		pageSize = 100
	}
	stride := pageSize
	if remaining > 0 && remaining < pageSize && config.paginationType != "page" {
		pageSize = remaining
	}
	if config.limitParam != "" {
		params[config.limitParam] = strconv.Itoa(pageSize)
	}
	if config.cursorParam != "" {
		switch config.paginationType {
		case "offset":
			params[config.cursorParam] = strconv.Itoa(page * stride)
		case "page":
			params[config.cursorParam] = strconv.Itoa(page + 1)
		default:
			if cursor != "" {
				params[config.cursorParam] = cursor
			}
		}
	}
	return params
}

func resourceNextCursor(config resourceReadConfig, current, next string, hasMore bool) (string, bool, error) {
	if config.paginationType == "offset" || config.paginationType == "page" {
		return current, false, nil
	}
	if next == "" {
		if hasMore {
			return "", false, fmt.Errorf("pagination reports more pages without an advanceable cursor")
		}
		return "", true, nil
	}
	if next == current {
		return "", false, fmt.Errorf("pagination cursor %q did not advance", current)
	}
	return next, false, nil
}
