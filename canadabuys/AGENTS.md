# CanadaBuys CLI

Data layer for CanadaBuys open CSVs. No auth. Do not scrape the HTML portal.

Commands: `datasets list`, `fetch`, `doctor`. Cache is `$XDG_CACHE_HOME/canadabuys` or `~/.cache/canadabuys`.

Run `gofmt`, `go vet`, and `go build ./...` before a PR. Stay inside `canadabuys/`.
