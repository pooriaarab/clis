# Build artifacts
build/
bin/
*.mcpb
*-pp-cli
*-pp-mcp
# ...but never ignore the cmd/ source dirs, whose names match the binary globs above.
!**/cmd/
!**/cmd/**

# Local state (real runtime location is XDG dirs outside the repo, but ignore defensively)
*.db
credentials.toml
config.toml

# OS
.DS_Store
# Cursor cloud — local secrets stay local
.env.local
.env.*.local
